# ─── Variables de entorno (Vercel → Settings → Environment Variables) ───
# Solo se exponen al cliente las que empiezan con VITE_. Nunca pongas aquí el service_role.
VITE_SUPABASE_URL=https://TU-PROJECT-ID.supabase.co
VITE_SUPABASE_ANON_KEY=tu-anon-key-aqui

# ─── Secrets de Edge Functions (se cargan con `supabase secrets set`, NO en Vercel) ───
# RESEND_API_KEY=re_xxxxxxxx
# EMAIL_FROM=ShadowScale <no-reply@shadowscale.pro>
